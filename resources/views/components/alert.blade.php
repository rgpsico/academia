{{-- <select name="{{$name}}"  id="{{$id}}" class="{{$class}} {{'w-'.$width ?? ''}}" style="{{$style}}" >
    <option value="" selected>Selecione</option>
    @foreach ($alunos as $aluno)
        <option value=" {{ $aluno->id }}"> {{ $aluno->nome }}</option>
    @endforeach
</select> --}}
